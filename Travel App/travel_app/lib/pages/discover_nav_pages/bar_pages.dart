import 'package:flutter/material.dart';

class BarPages extends StatelessWidget {
  const BarPages({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Bar Page is here !!!s"),
      ),
    );
  }
}
